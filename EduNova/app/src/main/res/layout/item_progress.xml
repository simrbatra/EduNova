<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/text_progress_percentage"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="85%"
                android:textColor="@color/text_primary"
                android:textSize="18sp"
                android:textStyle="bold"
                tools:text="85%" />

            <TextView
                android:id="@+id/text_mastery_level"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@color/primary_light"
                android:paddingHorizontal="8dp"
                android:paddingVertical="4dp"
                android:text="Advanced"
                android:textColor="@color/primary"
                android:textSize="12sp"
                android:textStyle="bold"
                tools:text="Advanced" />
        </LinearLayout>

        <ProgressBar
            android:id="@+id/progress_bar_lesson"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:layout_marginTop="8dp"
            android:max="100"
            android:progress="85"
            android:progressTint="@color/primary"
            tools:progress="85" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/text_attempts"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="3 attempts"
                android:textColor="@color/text_hint"
                android:textSize="12sp"
                tools:text="3 attempts" />
        </LinearLayout>

        <TextView
            android:id="@+id/text_weak_areas"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Weak: Fractions"
            android:textColor="@color/error"
            android:textSize="12sp"
            android:visibility="gone"
            tools:text="Weak: Fractions"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/text_strong_areas"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Strong: Addition, Subtraction"
            android:textColor="@color/success"
            android:textSize="12sp"
            android:visibility="gone"
            tools:text="Strong: Addition, Subtraction"
            tools:visibility="visible" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>

